
const Hero = () => {
  return <section className="container flex items-center justify-center">
    <div className="flex items-center justify-start gap-2 uppercase">
      <div className="w-2 h-2 rounded-full bg-test!"></div>
      <span>Available for new projects</span>
    </div>
  </section>
}

export default Hero