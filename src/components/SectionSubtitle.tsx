const SectionSubtitle = ({ children }: { children: React.ReactNode }) => {
  return (
    <span className="font-jetbrains text-gradient-brand uppercase">
      {children}
    </span>
  )
}

export default SectionSubtitle
